/**
 * TBD. This is a PLACEHOLDER for e2e tests for the main Inquirer process.
 * I haven't yet identified a testing harness that can operate on a CLI
 * tool, yet. Eventually, though, this file should contain full e2e cases
 * that verify the generator works in a variety of both successful and
 * unsuccessful cases, with different combinations of options and values
 * being specified to the tool.
 * 
 * @module
 */

describe('A Test Data Generator CLI Tool', () => {
  
  // verify that Jest is correctly configured
  it('can blow smoke', () => {
    expect(true).toBe(true)
  })
  
  // SKIPPED. (remove the 'x' when it's ready to be run)
  xit('can parse a JSON file with column specifications', () => {
    
  })
})
